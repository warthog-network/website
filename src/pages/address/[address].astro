---
import ExplorerApp from "../../components/explorer/ExplorerApp.jsx";
import BaseLayout from "../../layouts/BaseLayout.astro";
const { address } = Astro.params;
---
<BaseLayout title={`Address Transactions: ${address}`} bodyClass="site-body--wartbunker">
  <ExplorerApp client:only="react" />
</BaseLayout>
