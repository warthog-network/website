---
import TinyWart from "../BalkyImages/Isolation_Mode.svg";
import Coin from "../BalkyImages/001-coin.svg";
import Goal from "../BalkyImages/goal 1.svg";
import Sprout from '../BalkyImages/002-sprout.svg';
import Profile from '../BalkyImages/004-profile.svg';
import Programming from '../BalkyImages/006-programming.svg';
import Trolly from '../BalkyImages/005-trolley.svg';
import Group from '../BalkyImages/Group 5.svg';
import ContentDiv from "../components/ContentDiv.jsx";
---

<style>
    .font{
        font-family: 'Montserrat';
    }
    /* Slide-in animation styles */
    .animate-slide-in {
        opacity: 0;
        transform: translateX(-50px);
        transition: opacity 0.5s ease-out, transform 1s ease-out;
    }
    .animate-slide-in.visible {
        opacity: 1;
        transform: translateX(0);
    }
</style>
<div class="[text-shadow:_5px_1px_5px_rgb(0_0_0_)] bg-white text-white bg-[radial-gradient(circle_at_24.1%_68.8%,_rgb(50,50,50)_0%,_rgb(0,_0,_0)_99.4%)] pb-20">
  <ContentDiv>
    <header class="p-8">
        <p class="animate-slide-in text-2xl font-semibold">INNOVATION</p>
        <h1 class="animate-slide-in inline-flex mb-12 font-semibold text-3xl md:text-6xl">
            What makes us unique?
            <img class="relative right-4 pl-4 max-w-20" src={TinyWart.src} />
        </h1>
    </header>
    <div class="text-sm px-5 grid grid-cols-1 justify-center lg:grid-cols-3 mx-auto">
        <section class=" mb-10 lg:max-w-72 max-w-96">
            <h3 class="animate-slide-in my-2 flex font-bold text-base">TOKENOMICS<img class="pl-4" src={Coin.src} /></h3>
            <p class="animate-slide-in text-sm pr-4">
                This is not the default kind of project where you see a
                disproportionately high allocation of the total supply unfairly
                reserved for the team, the advisors and their pockets. 0% is
                minted, premined, reserved for anyone, 100% is publicly
                mineable.
            </p>
        </section>
        <section class=" lg:col-span-2 grid sm:grid-cols-2 grid-cols-1 sm:mx-auto">
            <div class="">
                <h2 class="animate-slide-in flex font-bold text-base sm:pt-2 pt-12">No Pre-mine <img class="pl-4" src={Goal.src} /></h2>
                <p class="animate-slide-in pb-2 pr-4">Project was fairly launched on Bitcointalk</p>

                <h2 class="animate-slide-in flex font-bold text-base sm:pt-2 pt-1">Written from scratch <img class="pl-4" src={Sprout.src} /></h2>
                <p class="animate-slide-in pb-2 pr-4">This project is NOT a fork/copy of anything else</p>
                <h2 class="animate-slide-in flex font-bold text-base sm:pt-2 pt-1">No reserved 'Team/Dev' fund <img class="pl-4" src={Profile.src} /></h2>
                <p class="animate-slide-in pb-2 pr-4">
                    No unfair allocation to anybody aka "team" / "development"
                    funds
                </p>
            </div>
            <div class="">
                <h2 class="animate-slide-in flex font-bold text-base sm:pt-2 pt-1">Modern Codebase <img class="pl-4" src={Programming.src} /></h2>
                <p class="animate-slide-in pb-2 pr-4">Using modern C++20 and tools like clanged, meson, ninja</p>
                <h2 class="animate-slide-in flex font-bold text-base sm:pt-2 pt-1">100% mineable <img class="pl-4" src={Trolly.src} /></h2>
                <p class="animate-slide-in pb-2 pr-4">100% of supply is publicly mineable, no coins were minted</p>
                <h2 class="animate-slide-in flex font-bold text-base sm:pt-2 pt-1">Open Source <img class="pl-4" src={Group.src} /></h2>
                <p class="animate-slide-in pb-2 pr-4">You are welcome to contribute</p>
            </div>
        </section>
    </div>
  </ContentDiv>
</div>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        const elements = document.querySelectorAll<HTMLElement>('.animate-slide-in');
        elements.forEach((item, index) => {
            item.style.transitionDelay = `${index * 0.2}s`;
        });

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, {
            threshold: 0.1
        });

        elements.forEach(item => {
            observer.observe(item);
        });
    });
</script>